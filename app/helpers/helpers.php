<?php

//function for store the student activity
if(!function_exists('StoreQuickActivity')){
    function StoreQuickActivity($student_id, $string){
        // $activity = new StudentActivity();
        // $activity->student_id = $student_id;
        // $activity->activity_string = $string;
        // $activity->save();
    }
}
